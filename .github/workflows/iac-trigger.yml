name: IaC Trigger

on:
  push:
    branches: ["main"]
    paths:
      - "iac/**"
      - '.github/**'
  pull_request:
    branches: ["main"]
    paths:
      - "iac/**"
      - '.github/**'

jobs:
  iac:
    uses: ./.github/workflows/iac.yml
    with:
      environment: dev
    secrets: inherit
